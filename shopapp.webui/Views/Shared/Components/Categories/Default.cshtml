@model List<Category>

<div class="list-group">
        <a href="/products" class="list-group-item list-group-item-action "> Tüm Kategoriler</a>

    @foreach (var C in Model)
    {        
        <a asp-controller="shop" 
            asp-action="list"
            asp-route-category ="@C.Url"
            class="list-group-item list-group-item-action @(ViewBag.SelectedCategory== C.Url?"active":"" ) " > @C.Name</a>
    }    
</div>